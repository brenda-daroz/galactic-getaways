

  <% @spaceships.each do |spaceship| %>
      <div>
      <%# <%= cl_image_path spaceship.photo.key, crop: :thumb %> %>
      <%= link_to spaceship.name, spaceship_path(spaceship) %>
      </div>
  <% end %>
